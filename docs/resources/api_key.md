---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "aptabase_api_key Resource - aptabase"
subcategory: ""
description: |-
  An aptabase-plus API key.
  ~> The plaintext key is only ever returned at creation. It is stored in Terraform state (as any secret-managing resource's state must) but aptabase-plus itself cannot return it again. If it's lost outside of state, there is no way to recover it - taint and recreate the resource to get a new one. This resource does not support import for the same reason: an imported key's plaintext can never be populated.
  ~> Every attribute forces replacement on change - aptabase-plus has no update endpoint for keys.
---

# aptabase_api_key (Resource)

An aptabase-plus API key.

~> **The plaintext key is only ever returned at creation.** It is stored in Terraform state (as any secret-managing resource's state must) but aptabase-plus itself cannot return it again. If it's lost outside of state, there is no way to recover it - taint and recreate the resource to get a new one. This resource does not support import for the same reason: an imported key's plaintext can never be populated.

~> Every attribute forces replacement on change - aptabase-plus has no update endpoint for keys.

## Example Usage

```terraform
resource "aptabase_api_key" "ci" {
  name = "ci-automation"
}

output "ci_api_key" {
  value     = aptabase_api_key.ci.key
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Key name.

### Optional

- `expires_at` (String) RFC3339 expiry timestamp. Omit for a key that never expires.

### Read-Only

- `id` (String) Key ID, assigned by aptabase-plus.
- `key` (String, Sensitive) The plaintext API key. Only populated on create - see the warning above.
- `key_prefix` (String) First 12 characters of the key, for identification without exposing the secret.
