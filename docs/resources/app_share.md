---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "aptabase_app_share Resource - aptabase"
subcategory: ""
description: |-
  Shares an aptabase-plus app with another user by email. There is no update - changing either app_id or email replaces the resource.
---

# aptabase_app_share (Resource)

Shares an aptabase-plus app with another user by email. There is no update - changing either `app_id` or `email` replaces the resource.

## Example Usage

```terraform
resource "aptabase_app_share" "example" {
  app_id = aptabase_app.example.id
  email  = "teammate@example.com"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `app_id` (String) ID of the shared app.
- `email` (String) Email address to share the app with.

### Read-Only

- `id` (String) `{app_id}/{email}`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import aptabase_app_share.example <app_id>/<email>
```
